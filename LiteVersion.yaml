tag: v1.0.7
version: v1.0.7
description: |
  ### Added
  - Add QR Code login. Login sessions are stored locally via DepotDownloaderMod and persist across restarts (sessions may expire over time).
  - Add a clickable icon next to the application dropdown to open the selected app's Workshop page.
  - PKG extraction now also handles `.tex` files.

  ### Changed
  - File integration is now available during downloads.
lastEdit: 2026-09-11 22:30
