tag: v1.0.6
version: v1.0.6
description: |
  ### Added
  - Automatically search for the path `projects/myprojects` and use it as the default save path.
  - Automatically clean up the `.DepotDownloader` files created after DepotDownloader finishes downloading.

  ### Removed
  - Version migration adaptation for changes to default filenames.

  ### Changed
  - Significantly reduce the application IDs, keeping only the popular ones.
lastEdit: 2026-01-25 08:30
